#include <bits/stdc++.h>

using namespace std;

int solve(string s, int n)
{
    
}

int main()
{
    int testcase;
    cin >> testcase;
    while (testcase--)
    {
        string s;
        cin >> s;
        cout << solve(s, s.size());
    }
    return 0;
}