#include <iostream>

using namespace std;
#define MAX (int(1e5))

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n;
        int count = 0;
        for (int i = 0; i <= MAX;i++)
        {
            int ans = 0;
            if()
            {
                count++;
                if(count == n)
                {
                    cout << ans;
                    break;
                }
            }
        }
    }
    return 0;
}