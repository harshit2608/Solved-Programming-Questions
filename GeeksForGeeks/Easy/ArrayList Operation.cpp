/*This program has to be submitted in java so its on pause for now*/

#include <iostream>

using namespace std;
#define MAX (int(1e5))

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n,arr[MAX],i=0;
        cin >> n;
        while(i<n)
        {
            cin >> arr[i];
            i++;
        }
    }
}